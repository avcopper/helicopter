package com.andrew.helicopter.System;

public class SQLite {
    public SQLite() {
//        SQLiteDatabase dbs = getBaseContext().openOrCreateDatabase("app.db", MODE_PRIVATE, null);
//        dbs.execSQL("CREATE TABLE IF NOT EXISTS helicopters (id INTEGER, number TEXT)");
//        dbs.execSQL("INSERT OR IGNORE INTO helicopters VALUES (1, '165'), (2, '184'), (3, '193');");
//        Cursor query = dbs.rawQuery("SELECT * FROM helicopters;", null);
//
//        while(query.moveToNext()){
//            int id = query.getInt(0);
//            String number = query.getString(1);
//            System.out.println ("id: " + id + " number: " + number + "\n");
//        }
//        query.close();
//        dbs.close();
    }
}
